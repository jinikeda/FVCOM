      subroutine ice_init_0
      implicit none
      ! Dummy ice initializer: FVCOM expects this symbol, but we
      ! don't actually need to initialize any ice model fields.
      return
      end
